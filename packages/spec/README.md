# @dsh-skins/spec

Client-independent manifest types, JSON Schemas, visual slot catalog, validation, fallback resolution, and renderer capability negotiation for DshSkins protocol version 1.

Skin manifests contain data and local raster asset references only. They cannot contain JavaScript, CSS, remote asset URLs, model prompts, or tool permissions.
